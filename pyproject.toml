[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "claude-smt"
version = "0.1.0"
description = "An SMT solver supporting Boolean, bitvector, integer, real, and string theories"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
